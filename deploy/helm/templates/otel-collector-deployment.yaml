---
# OpenTelemetry Collector for Edge Logs with K8s metadata extraction
apiVersion: v1
kind: ConfigMap
metadata:
  name: otel-collector-config
  namespace: {{ include "edge-logs.namespace" . }}
data:
  otel-collector-config.yaml: |
    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
          http:
            endpoint: 0.0.0.0:4318

    processors:
      memory_limiter:
        check_interval: 1s
        limit_mib: 150
        spike_limit_mib: 30

      # Transform processor to extract K8s metadata from __path__
      # Use resource context to extract and add to resource attributes
      transform:
        log_statements:
          # First, extract from resource context to modify resource attributes
          - context: resource
            statements:
              # Extract pod name: /var/log/containers/<pod>_<namespace>_<container>-<hash>.log
              # Split by "/" to get filename, then split by "_" and take first part (pod name)
              - set(attributes["k8s.pod.name"], Split(Split(attributes["__path__"], "/")[4], "_")[0])
              # Extract namespace name
              - set(attributes["k8s.namespace.name"], Split(attributes["__path__"], "_")[1])
              # Extract container name
              - set(attributes["k8s.container.name"], Split(Split(attributes["__path__"], "_")[2], "-")[0])

      batch:
        timeout: 1s
        send_batch_size: 1024
        send_batch_max_size: 2048

      resource:
        attributes:
          - key: deployment.environment
            value: "production"
            action: upsert

    exporters:
      clickhouse:
        endpoint: tcp://{{ include "edge-logs.clickhouse.serviceName" . }}:9000
        database: edge_logs
        logs_table_name: otel_logs
        create_schema: false
        timeout: 10s
        retry_on_failure:
          enabled: true
          initial_interval: 5s
          max_interval: 30s
          max_elapsed_time: 300s

      # Debug exporter disabled to prevent 73GB log bloat
      # Uncomment only for troubleshooting, then remove immediately
      # debug:
      #   verbosity: normal

    extensions:
      health_check:
        endpoint: 0.0.0.0:13133

    service:
      extensions: [health_check]

      pipelines:
        logs:
          receivers: [otlp]
          processors: [memory_limiter, transform, batch]
          exporters: [clickhouse]

      telemetry:
        logs:
          level: "warn"  # Reduced from "info" to prevent excessive logging
        metrics:
          level: "basic"

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: otel-collector
  namespace: {{ include "edge-logs.namespace" . }}

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: otel-collector
rules:
  - apiGroups: [""]
    resources: ["pods", "namespaces", "nodes"]
    verbs: ["get", "list", "watch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: otel-collector
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: otel-collector
subjects:
  - kind: ServiceAccount
    name: otel-collector
    namespace: {{ include "edge-logs.namespace" . }}

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: otel-collector
  namespace: {{ include "edge-logs.namespace" . }}
  labels:
    app: otel-collector
    component: data-pipeline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: otel-collector
  template:
    metadata:
      labels:
        app: otel-collector
        component: data-pipeline
    spec:
      serviceAccountName: otel-collector
      {{- if .Values.otelCollector.initContainer.enabled }}
      initContainers:
        # Step 1: Wait for ClickHouse TCP port using busybox (has nc command)
        - name: wait-for-clickhouse
          image: busybox:1.36
          command: ['sh', '-c']
          args:
            - |
              CLICKHOUSE_HOST="{{ include "edge-logs.clickhouse.serviceName" . }}.{{ include "edge-logs.namespace" . }}.svc.cluster.local"
              CLICKHOUSE_PORT=9000

              echo "Waiting for ClickHouse at $CLICKHOUSE_HOST:$CLICKHOUSE_PORT..."
              until nc -z $CLICKHOUSE_HOST $CLICKHOUSE_PORT; do
                echo "ClickHouse not ready, waiting..."
                sleep 2
              done
              echo "ClickHouse TCP port is ready!"
              sleep 10
        # Step 2: Execute SQL scripts using ClickHouse client
        - name: init-clickhouse-tables
          image: {{ include "edge-logs.clickhouse.image" . }}
          imagePullPolicy: {{ .Values.clickhouse.image.pullPolicy }}
          command: ['sh', '-c']
          args:
            - |
              set -e
              CLICKHOUSE_HOST="{{ include "edge-logs.clickhouse.serviceName" . }}.{{ include "edge-logs.namespace" . }}.svc.cluster.local"
              CLICKHOUSE_PORT=9000

              echo "Executing ClickHouse initialization scripts..."
              for sql_file in /docker-entrypoint-initdb.d/*.sql; do
                if [ -f "$sql_file" ]; then
                  echo "Executing $sql_file..."
                  clickhouse-client --host $CLICKHOUSE_HOST --port $CLICKHOUSE_PORT --multiquery < "$sql_file" || {
                    echo "Failed to execute $sql_file, retrying once..."
                    sleep 5
                    clickhouse-client --host $CLICKHOUSE_HOST --port $CLICKHOUSE_PORT --multiquery < "$sql_file"
                  }
                fi
              done
              echo "ClickHouse initialization completed successfully"
          volumeMounts:
            - name: clickhouse-init-scripts
              mountPath: /docker-entrypoint-initdb.d
          resources:
            {{- toYaml .Values.otelCollector.initContainer.resources | nindent 12 }}
      {{- end }}
      containers:
        - name: otel-collector
          image: {{ include "edge-logs.otelCollector.image" . }}
          imagePullPolicy: IfNotPresent
          command:
            - "/otelcol-contrib"
            - "--config=/etc/otelcol-contrib/otel-collector-config.yaml"
          ports:
            - name: otlp-grpc
              containerPort: 4317
            - name: otlp-http
              containerPort: 4318
            - name: health
              containerPort: 13133
            - name: metrics
              containerPort: 8888
          env:
            - name: GOMEMLIMIT
              value: "160MiB"
            - name: OTLP_LOG_LEVEL
              value: "warn"  # Minimize log output to prevent disk space issues
          resources:
            limits:
              memory: "256Mi"
              cpu: "500m"
            requests:
              memory: "128Mi"
              cpu: "100m"
          volumeMounts:
            - mountPath: /etc/otelcol-contrib/otel-collector-config.yaml
              name: otel-collector-config
              subPath: otel-collector-config.yaml
          livenessProbe:
            httpGet:
              path: /
              port: 13133
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 13133
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        {{- if .Values.otelCollector.initContainer.enabled }}
        - configMap:
            name: {{ include "edge-logs.fullname" . }}-clickhouse-init
          name: clickhouse-init-scripts
        {{- end }}
        - configMap:
            items:
              - key: otel-collector-config.yaml
                path: otel-collector-config.yaml
            name: otel-collector-config
          name: otel-collector-config

---
apiVersion: v1
kind: Service
metadata:
  name: otel-collector
  namespace: {{ include "edge-logs.namespace" . }}
  labels:
    app: otel-collector
    component: data-pipeline
spec:
  type: ClusterIP
  ports:
    - name: otlp-grpc
      port: 4317
    - name: otlp-http
      port: 4318
    - name: metrics
      port: 8888
  selector:
    app: otel-collector
